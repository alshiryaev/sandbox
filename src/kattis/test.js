// 2, 212, 1253

const readline = require('readline');

const rl = readline.createInterface({
    input: process.stdin,
    output: process.stdout
});

rl.

rl.on('line', (line) => {

    // const [, ...arr] = nums;
    // let sum = 0;
    // for (const item of arr) {
      
    //   const digit = item % 10;
    //   const result = Math.floor(item / 10);
    //   sum = sum + Math.pow(result, digit);
    // }

    // console.log(sum);

    console.log(line);
    
});

rl.on('close', () => console.log('close'))
    

